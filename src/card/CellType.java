package card;

/**
 * Represents the different types of cells in a game grid.
 * <ul>
 *   <li>HOLE - A cell that represents a hole.</li>
 *   <li>CARD - A cell that contains a card.</li>
 *   <li>EMPTY - A cell that is empty.</li>
 * </ul>
 */
public enum CellType {
  HOLE,  // Represents a hole in the game grid.
  CARD,  // Represents a cell containing a card.
  EMPTY; // Represents an empty cell.
}

